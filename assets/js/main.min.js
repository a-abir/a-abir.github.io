(()=>{'use strict';const $=(s,r=document)=>r.querySelector(s);const $$=(s,r=document)=>[...r.querySelectorAll(s)];const root=document.documentElement;const rmq=matchMedia('(prefers-reduced-motion: reduce)');const reduced=()=>rmq.matches;const onMQ=(mq,fn)=>mq.addEventListener?mq.addEventListener('change',fn):mq.addListener(fn);const setTheme=t=>{root.dataset.theme=t;try{localStorage.setItem('theme',t);}catch{}$('#theme-toggle')?.setAttribute('aria-label',`Switch to ${t === 'dark' ? 'light' : 'dark'} theme`);};$('#theme-toggle')?.addEventListener('click',()=>setTheme(root.dataset.theme==='dark'?'light':'dark'));const nav=$('.nav'),bar=$('.progress');let scrollRange=0;let queued=false;const measure=()=>{scrollRange=Math.max(0,document.documentElement.scrollHeight-innerHeight);};const paint=()=>{queued=false;const y=window.scrollY;if(nav)nav.dataset.scrolled=y>8;if(bar)bar.style.transform=`scaleX(${scrollRange ? y / scrollRange : 0})`;};const onScroll=()=>{if(queued)return;queued=true;requestAnimationFrame(paint);};addEventListener('scroll',onScroll,{passive:true});addEventListener('resize',()=>{measure();onScroll();},{passive:true});requestAnimationFrame(()=>{measure();paint();});const burger=$('#nav-toggle');const mobile=$('#nav-mobile');let closeMenu=()=>{};if(burger&&mobile){const isOpen=()=>burger.getAttribute('aria-expanded')==='true';const setMenu=open=>{burger.setAttribute('aria-expanded',String(open));burger.setAttribute('aria-label',open?'Close menu':'Open menu');mobile.hidden=!open;};closeMenu=()=>setMenu(false);burger.addEventListener('click',()=>setMenu(!isOpen()));mobile.addEventListener('click',e=>{if(e.target.closest('a'))setMenu(false);});addEventListener('keydown',e=>{if(!isOpen())return;if(e.key==='Escape'){setMenu(false);burger.focus();return;}if(e.key!=='Tab')return;const f=[burger,...$$('a',mobile)];const first=f[0],last=f[f.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus();}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus();}});document.addEventListener('click',e=>{if(!isOpen())return;if(!e.target.closest('#nav-mobile')&&!e.target.closest('#nav-toggle'))setMenu(false);});addEventListener('pageshow',()=>setMenu(false));onMQ(matchMedia('(min-width: 761px)'),e=>{if(e.matches)setMenu(false);});}const spy=linkSel=>{const links=$$(linkSel).filter(a=>a.hash&&$(a.hash));if(!links.length)return;const map=new Map(links.map(a=>[$(a.hash),a]));const seen=new Set();const io=new IntersectionObserver(es=>{es.forEach(e=>e.isIntersecting?seen.add(e.target):seen.delete(e.target));const first=[...map.keys()].find(t=>seen.has(t));links.forEach(a=>a.removeAttribute('aria-current'));if(first)map.get(first).setAttribute('aria-current','true');},{rootMargin:'-15% 0px -70% 0px',threshold:0});map.forEach((_,t)=>io.observe(t));};spy('.nav__links a');spy('.nav__mobile a');spy('.toc a');const revealables=$$('.reveal');const onscreen=revealables.filter(el=>el.getBoundingClientRect().top<innerHeight);onscreen.forEach(el=>el.classList.add('in'));if(reduced())revealables.forEach(el=>el.classList.add('in'));else{const ro=new IntersectionObserver((es,obs)=>{es.forEach(e=>{if(!e.isIntersecting)return;e.target.classList.add('in');obs.unobserve(e.target);});},{rootMargin:'0px 0px -8% 0px',threshold:.08});revealables.forEach(el=>ro.observe(el));}const hl=$('#headline');if(hl){let lines=[];try{lines=JSON.parse(hl.dataset.lines||'[]');}catch{}const out=$('#headline-text',hl);if(out&&lines.length){const KEY='headline:i';let i=0;try{i=(parseInt(localStorage.getItem(KEY),10)||0)%lines.length;localStorage.setItem(KEY,String((i+1)%lines.length));}catch{i=Math.floor(Math.random()*lines.length);}out.textContent=lines[i];}else if(out){out.textContent='';}$('.cursor',hl)?.remove();}if(!reduced()&&matchMedia('(hover: hover)').matches){$$('.card').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect();card.style.setProperty('--mx',`${e.clientX - r.left}px`);card.style.setProperty('--my',`${e.clientY - r.top}px`);});card.addEventListener('pointerenter',()=>{card.style.borderColor='var(--border-strong)';card.style.transform='translateY(-3px)';card.style.boxShadow='var(--shadow)';});card.addEventListener('pointerleave',()=>{card.style.borderColor='';card.style.transform='';card.style.boxShadow='';});});}const pal=$('#palette');let closePalette=()=>{};if(pal){const input=$('#palette-input');const list=$('#palette-list');const items=$$('li',list);let empty=null;const visible=()=>items.filter(li=>!li.hidden);const clearActive=()=>$$('a.is-active',list).forEach(a=>a.classList.remove('is-active'));const open=()=>{if(pal.open)return;pal.showModal();input.value='';items.forEach(li=>li.hidden=false);empty?.remove();empty=null;clearActive();input.focus();};const close=()=>pal.open&&pal.close();closePalette=close;$$('[data-open-palette]').forEach(b=>b.addEventListener('click',open));addEventListener('keydown',e=>{const typing=/input|textarea|select/i.test(document.activeElement?.tagName||'');if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='k'){e.preventDefault();pal.open?close():open();}else if(e.key==='/'&&!typing&&!pal.open){e.preventDefault();open();}});pal.addEventListener('pointerdown',e=>{const box=$('.pal__box',pal).getBoundingClientRect();const outside=e.clientX<box.left||e.clientX>box.right||e.clientY<box.top||e.clientY>box.bottom;if(outside)close();});pal.addEventListener('click',e=>{if(e.target===pal)close();});input.addEventListener('input',()=>{const q=input.value.trim().toLowerCase();items.forEach(li=>li.hidden=q&&!li.textContent.toLowerCase().includes(q));clearActive();const none=visible().length===0;if(none&&!empty){empty=document.createElement('p');empty.className='pal__empty';empty.textContent='no match. try fewer letters.';list.after(empty);}else if(!none&&empty){empty.remove();empty=null;}});list.addEventListener('click',e=>{if(e.target.closest('a'))close();});const move=dir=>{const links=visible().map(li=>$('a',li));if(!links.length)return;const cur=links.findIndex(a=>a.classList.contains('is-active'));const next=dir>0?(cur+1)%links.length:(cur<=0?links.length-1:cur-1);clearActive();links[next].classList.add('is-active');links[next].scrollIntoView({block:'nearest'});};pal.addEventListener('keydown',e=>{if(e.key==='ArrowDown'){e.preventDefault();move(1);}else if(e.key==='ArrowUp'){e.preventDefault();move(-1);}else if(e.key==='Enter'){e.preventDefault();($('a.is-active',list)||$('a',visible()[0]||document.createElement('li')))?.click();}});pal.addEventListener('close',()=>{clearActive();empty?.remove();empty=null;});}const supportsVT='startViewTransition'in document&&typeof CSS!=='undefined'&&CSS.supports('view-transition-name: a');if(supportsVT){const KEY='vt:from-slug';const slugOf=url=>{try{const m=new URL(url,location.href).pathname.match(/\/projects\/([^/]+)\.html?$/i);return m?m[1]:null;}catch{return null;}};const hereSlug=()=>$('.proj-hero')?.dataset.slug||slugOf(location.href);const stash=v=>{try{v?sessionStorage.setItem(KEY,v):sessionStorage.removeItem(KEY);}catch{}};const unstash=()=>{try{const v=sessionStorage.getItem(KEY);sessionStorage.removeItem(KEY);return v;}catch{return null;}};const untag=()=>{$$('.vt-card').forEach(el=>{if(!el.classList.contains('proj-hero'))el.classList.remove('vt-card');});$$('.card .vt-title').forEach(el=>el.classList.remove('vt-title'));};const inViewport=el=>{const r=el.getBoundingClientRect();if(!r.width||!r.height)return false;const vh=innerHeight||root.clientHeight;const vw=innerWidth||root.clientWidth;const visY=Math.min(r.bottom,vh)-Math.max(r.top,0);const visX=Math.min(r.right,vw)-Math.max(r.left,0);return visY>Math.min(r.height*0.35,80)&&visX>0;};const tagCard=slug=>{untag();if(!slug)return false;const card=document.querySelector(`.card[data-slug="${CSS.escape(slug)}"]`);if(!card||!inViewport(card))return false;card.classList.add('vt-card');card.querySelector('h3 a')?.classList.add('vt-title');return true;};const settleFragment=()=>{const id=location.hash;if(!id||id==='#')return;let target=null;try{target=document.querySelector(id);}catch{return;}if(!target)return;const prev=root.style.scrollBehavior;root.style.scrollBehavior='auto';target.scrollIntoView({block:'start'});root.style.scrollBehavior=prev;};const shutOverlays=()=>{try{closeMenu();closePalette();}catch{}};addEventListener('pageswap',e=>{if(!e.viewTransition){stash(null);return;}if(reduced()){e.viewTransition.skipTransition();untag();stash(null);return;}shutOverlays();const to=e.activation?.entry?.url||location.href;const from=hereSlug();stash(!slugOf(to)&&from?from:null);root.dataset.vtDir=slugOf(to)?'forward':'back';if(slugOf(to)&&!from)tagCard(slugOf(to));e.viewTransition.finished.then(untag,untag);});addEventListener('pagereveal',e=>{if(!e.viewTransition){root.removeAttribute('data-vt-arriving');unstash();return;}if(reduced()){e.viewTransition.skipTransition();root.removeAttribute('data-vt-arriving');root.removeAttribute('data-vt-dir');untag();unstash();return;}const here=hereSlug();const from=unstash();root.dataset.vtArriving='';root.dataset.vtDir=here?'forward':'back';if(!here){settleFragment();if(from)tagCard(from);}const cleanup=()=>{root.removeAttribute('data-vt-arriving');root.removeAttribute('data-vt-dir');untag();};e.viewTransition.finished.then(cleanup,()=>root.removeAttribute('data-vt-arriving'));setTimeout(cleanup,1200);});addEventListener('pagehide',()=>{untag();shutOverlays();});}console.log('%c> abrian abir%c\n  built by hand. no framework, no build step at runtime.\n  content lives in /data/site.json — run `python build.py` to regenerate.\n  if you are reading this, we will probably get along.','color:#4FD1C5;font-weight:700;font-family:monospace;font-size:13px','color:#8B949E;font-family:monospace;font-size:12px');})();